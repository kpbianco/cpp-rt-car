# Profile fixtures

The checked-in JSON profile is an illustrative autotune fixture. The current
runtime does not load it, and its values are not a validated recommendation for
the named CPU. Runtime profile integration is tracked in `docs/roadmap.md`.
