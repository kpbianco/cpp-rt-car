# Profile fixtures

The checked-in JSON profile is an illustrative autotune fixture. RTFW 0.1 does
not load it, and its values are not a validated recommendation for the named
CPU. Runtime profile integration is tracked in `docs/roadmap.md`.
