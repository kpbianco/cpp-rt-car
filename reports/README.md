# Report fixtures

Tracked reports are illustrative tooling fixtures unless the report itself
records a supported executable and complete provenance. The current demo is not
integrated with the default runtime autotune spec.
