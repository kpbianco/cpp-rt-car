# M17-06 retained implementation evidence

Date: 2026-08-23 UTC

Batch: M17-06, Repair Runtime capability discovery and complete the portable
combined graph sample

Baseline: `875c486570ed84df939b8b9e75c01e7c3d43b28b`

Control revision: `acdbdc50f66fd66f9a8e48eee75923edfd759787`

Validation class: static, portable build/package, unit, injected-driver,
failure-injected, sanitizer-build, ASan/UBSan execution, and RT0 simulated-
protocol functional evidence only.

## Baseline, history, and scope

`git rev-parse HEAD` remained the exact baseline throughout implementation.
The target began with only the canonical control activation edits in
`contracts/active-batch.yaml` and `contracts/repo-profile.yaml`; those edits
were preserved. No pre-existing product implementation edit was present.

Each of these commands returned exit 0:

- `git merge-base --is-ancestor
  bdc97d0122effbad07dbf6d4981e1bf0de729437 HEAD` (M17-04);
- `git merge-base --is-ancestor
  e5f10cc572fa9ba3d4dd488781c310dafb34dfd2 HEAD` (retained M17-05 review);
- `git merge-base --is-ancestor
  5eb8f101f692b2aff85f82f4a1660630db750706 HEAD` (M18-01);
- `git merge-base --is-ancestor
  d5224e670e3ca5ebbe3d2aec5fda7d21213499a8 HEAD` (M19-01).

Repository and canonical control artifacts were read before editing. The
canonical and target M17-06 contracts initially agreed except for the expected
injected control provenance. Both named nonexistent
`contracts/verification.commands.yaml`. Independent review corrected the
target locator to the authoritative existing `contracts/verification.commands`;
`contracts/verification.yaml` remains the profile definition.

Every changed path is inside the active-batch allowlist. No forbidden public
header, HAL/device-manager/host-runtime/executor/vendor implementation, ABI
allowlist/fingerprint, schema, qualification tool/workflow, support matrix,
version, license, prior evidence, Unreal, hardware sample, or hardware workflow
path changed. Three root-level files generated by full tests
(`chunk_cache_test.devbox`, `chunk_cache_test2.devbox`, and `trace.btrc`) were
removed after verification.

## Acceptance mapping

- The sole production repair is in `rt/src/command_batch.cpp`.
  `discover_command_timeline_extension()` now constructs
  `HalV2CommandTimelineCapabilities capabilities{};`. Frozen default member
  initializers provide exact `sizeof(HalV2CommandTimelineCapabilities)` and
  command/timeline extension version 1 on callback entry, while semantic and
  reserved fields start zero. The existing callback exception/status mapping,
  complete returned-record validation, and publish-after-validation behavior
  are unchanged. No public declaration, value, callback, vendor code, or other
  Runtime path changed.
- `CommandBatch.CapabilityDiscoverySeedsExactHeaderRejectsMalformedAndRetriesCleanly`
  observes the exact incoming header and zero semantic/reserved state. It
  rejects null instance/callback, throw, error, unsupported,
  resource-exhausted, untouched, partial, short-size, wrong-version, every
  zero required capacity, every excess public capacity, nonzero reserved data,
  and insufficient Runtime in-flight/completion capacity. Each rejected handle
  remains invalid and backend count remains zero; a corrected registration in
  the same configured Runtime then publishes exactly once.
- `VendorHalV2.NativeRuntimeRegistrationDiscoversBothCandidates` constructs
  the actual `CudaDeviceBackend` and `XdmaDeviceBackend` classes with complete
  version-2 injected driver tables, registers both native
  `hal_v2_registration()` records in one real Runtime, and reaches configure,
  finalize, start, step, checked stop, and stopped state. It does not call the
  capability callbacks directly or use a substitute wrapper/backend.
- The default build adds exactly one non-installed
  `sample_cpu_gpu_fpga_cpu` executable and one standalone
  `rtfw_cpu_gpu_fpga_cpu_sample_tests` executable/CTest gate. Both link only
  `rtfw_runtime`, `rtfw_cuda_backend`, and `rtfw_xdma_backend` and include only
  existing public headers. They build and run with experimental code disabled
  and without a CUDA toolkit, GPU, XDMA node/driver, privilege, firmware,
  bitstream, network, or secret.
- The graph has exactly five ordinary dependency phases: CPU prepare, CUDA
  batch, CPU host-stage bridge, XDMA batch, and CPU validate. CUDA and XDMA own
  distinct timelines with zero waits. No handle, signal, completion, memory
  object, fence, or value crosses a backend; cross-backend order is CUDA
  download completion, bounded CPU copy, then XDMA upload.
- Each 32-byte CUDA batch contains exactly copy-to-device, stable graph-ID 7
  dispatch with one copied binding, and copy-from-device, followed by one
  completion event and one monotonically increasing CUDA-local signal. Fixed
  opaque simulated device storage applies `input * 2 + 3` per element.
- The bridge checks the complete CUDA result, copies exactly 32 bytes between
  distinct `alignas(64)` CUDA and XDMA host objects, and verifies that no CUDA
  or XDMA driver count changes during the copy. The objects are disjoint,
  never dual-registered, and declare staged-copy or borrowed-host coherent
  semantics respectively.
- Each XDMA batch contains exact H2C to card offset 64, aligned 32-bit little-
  endian control write at offset 4, finite event wait 0, and C2H. Fixed
  simulated 256-byte card, four-word control, and event state add the
  frame-specific trigger to every integer. No node, BAR, PCI function, kernel
  driver, firmware, bitstream, or physical FPGA is touched.
- Two normal frames produce the exact trace
  `P U H G D E B X h w e c V` twice: CPU prepare/provider, CUDA
  upload/Graph/download/event, CPU bridge/XDMA provider, XDMA
  H2C/write/event/C2H, and CPU validation. Each backend reports accepted and
  completed values 1 then 2. Counts are exactly two Graph launches, two XDMA
  triggers/events, two validations, and four accepted-before-complete device
  batches. The executable prints exactly
  `sample_cpu_gpu_fpga_cpu frames=2 evidence=simulated_protocol
  physical_hardware=false direct_peer_dma=false` and returns nonzero on any
  mismatch.
- Payloads, graph bindings, simulated device/card/control/event state, batch
  declarations, trace storage, failure state, and per-frame observations are
  compile-time bounded or initialization-time fixed. Allocation tracking begins
  after successful `Runtime::start()` and covers complete `step()` calls;
  success and injected failures report zero ordinary/aligned heap allocations.
- Thread identity proves the three CPU phases use no more than the configured
  two executor workers, CUDA enqueue calls use one Runtime submission owner,
  CUDA event queries use the distinct Runtime service owner, and XDMA calls use
  one of its two fixed worker threads. No vendor callback uses an executor or
  host-control thread. The CPU policy report is exactly two executor, two
  submission, and one service instance; no sample thread, lane, spill, detached
  task, I/O, or unbounded wait is added.
- Two independent complete fixtures prove foreign-handle rejection and isolate
  native registration paths, graph-exec handles, buffers, card/control/event
  state, timelines, traces, stop flags, failures, and cleanup. One fixture is
  verified unchanged while the other times out and recovers, then independently
  completes two successful frames.
- CUDA Graph failure occurs after upload and yields exact `P U H G`: no CUDA
  completion/download, bridge, XDMA work, or validation. Runtime reset performs
  the existing stream synchronization before checked cleanup, and a fresh
  fixture completes successfully.
- XDMA event timeout occurs after successful CPU/CUDA/bridge and exact
  `P U H G D E B X h w e`: CUDA is accepted/completed 1/1 and XDMA is
  accepted/completed 1/0; no C2H, XDMA timeline publication, or validation
  occurs. The stop request wakes and cancels the finite event. Bounded
  CUDA-only recovery steps reap the candidate's late canceled completion before
  a corrected same-fixture frame succeeds; a separate fixture remains isolated
  and then completes normally.
- Zero timeout and foreign CUDA signal declarations fail before any native
  driver call. Checked XDMA shutdown failure retains the first error; a second
  stop retries only the unresolved XDMA cleanup and does not repeat successful
  CUDA buffer/free cleanup.
- Existing accepted-before-complete, whole-completion, timeout/cancel,
  first-error, reverse cleanup, quarantine, caller-owned Graph/buffer,
  extension-readiness, and no-detached-work behavior is preserved. The sample
  adds no Runtime/backend-owned storage, MemoryPlan row, provider region,
  status, schema, installed target/header, support record, or support claim.

## Independent system-risk review and repairs

- Medium, repaired: the XDMA timeout test observed timeline non-publication
  immediately after the Runtime synthesized the timeout, but did not observe
  it while the late canceled backend completion was being reaped or immediately
  before corrected reuse. A regression could therefore have advanced the
  backend-local completed value transiently and escaped the final recovery
  assertion. `tests/test_cpu_gpu_fpga_cpu_sample.cpp` now checks accepted value
  1 and completed value 0 after every bounded reaping step and once more before
  reuse. The strengthened test passed 20 consecutive executions and its
  ASan/UBSan execution.
- Medium, repaired: the active batch named nonexistent
  `contracts/verification.commands.yaml`, creating an ambiguous deterministic-
  gate locator for reviewers and automation. The allowlisted target contract
  now names the existing authoritative `contracts/verification.commands`.
- Low, repaired: the real-time readiness text still assigned pending API,
  compatibility, concurrency, accounting, lifecycle, and claim review to
  completed M17-02. It now assigns the external gates to M17-06, keeps M17 and
  CAP-M17 incomplete, and explicitly preserves the physical/hardware/RT claim
  boundary.
- No unresolved material correctness, concurrency, lifetime, resource,
  migration, compatibility, security, licensing, recovery, package/public-
  interface, or missing-test finding remained in the reviewed diff. This is a
  local independent review only, not the required hosted CI or recorded human
  approval gate. One local sanitizer-validation discrepancy remains and is
  reported below without treating it as passing evidence.

## Exact verification results

- Initial `./scripts/agent-verify.sh contract` — exit 0; retained log
  `docs/evidence/local/verify-contract-20260823T111436Z.log`.
- Initial `./scripts/agent-verify.sh quick` — exit 0; retained log
  `docs/evidence/local/verify-quick-20260823T111437Z.log`.
- Exact active-batch RelWithDebInfo configure and full build with tests,
  experimental code, RapidCheck, empty sanitizer set, and `SIM_WERROR=ON` —
  exit 0.
- `./build/agent-full/tests/simcore_tests
  --gtest_filter='CommandBatch.*Capability*:VendorHalV2.*RuntimeRegistration*'`
  — exit 0; 2/2 focused tests passed.
- `./build/agent-full/tests/rtfw_cpu_gpu_fpga_cpu_sample_tests` — exit 0 and
  printed its stable simulated-protocol success line.
- `./build/agent-full/samples/sample_cpu_gpu_fpga_cpu` — exit 0 and printed the
  exact two-frame success line.
- Exact focused CTest regex from the active batch — exit 0; 16/16 gates passed,
  including M15, M16, M17, M19, standalone XDMA, and both M17-06 gates.
- Exact `ctest --repeat until-fail:20` M17-06 regex — exit 0; both executables
  passed all 20 repetitions. An additional direct focused stress loop passed
  100/100 executions after the bounded XDMA cancellation-handshake repair.
- Initial exact default-off ASan/UBSan configure/build commands — exit 0. All
  eight named sanitizer gates passed across the captured initial and equivalent
  individually named CTest invocations; no AddressSanitizer,
  UndefinedBehaviorSanitizer, or leak finding was reported.
- Exact default-off ThreadSanitizer configure/build commands — exit 0. The
  eight binaries could not execute in this sandbox: seven reported
  `FATAL: ThreadSanitizer: unexpected memory mapping` before test code and one
  was reported by CTest as a startup segmentation fault. This is not passing
  TSan evidence. The mandatory hosted TSan job remains required.
- Exact Release default-off package build/install/consumer commands — exit 0;
  installed consumer CTest passed 13/13. The install manifest contains no
  combined sample or test target.
- The first `./scripts/agent-verify.sh full` attempt passed contracts, quick,
  the full build, and 29/30 full CTests, but the pre-existing 405-test
  `simcore_all` aggregate reached its fixed 300-second timeout while entering
  its final legacy determinism case. Direct unchanged aggregate execution then
  completed in 24.830 seconds: 401 passed and four expected tests skipped.
- Final `./scripts/agent-verify.sh full` rerun — exit 0. Contract and release-
  tool checks passed, quick CTest passed 26/26, full CTest passed 30/30, and the
  405-test aggregate passed in 33.60 seconds. Retained log:
  `docs/evidence/local/verify-full-20260823T121617Z.log`. The failed first-attempt
  log is retained at
  `docs/evidence/local/verify-full-20260823T121000Z.log`; it is not omitted or
  represented as a pass.
- Final `./scripts/agent-verify.sh contract` after retained evidence and
  traceability links — exit 0; retained log
  `docs/evidence/local/verify-contract-20260823T122208Z.log`.
- `python3 tools/check_release_contract.py --write-hashes`,
  `python3 tools/check_release_contract.py`, and
  `python3 tools/check_docs_contract.py` — exit 0 after adding permanent M17-06
  gates and refreshing the reviewed source hashes.
- `python3 tools/check_c_abi.py --library build/agent-full/librtfw.so` — exit 0;
  C ABI v8, 70 exports, and fingerprint `0xd0e7a5a14bf35f97` verified.
- `readelf -d build/agent-full/librtfw.so | rg
  'SONAME.*librtfw.so.8'` — exit 0 and reported `librtfw.so.8`.
- Final baseline/ancestor checks, `git diff --check`, allowed/forbidden path
  audit, reviewed-hash validation, changed-text EOF audit, and exact changed-
  name inspection — exit 0.
- Independent-review `./scripts/agent-verify.sh contract` — exit 0; retained
  log `docs/evidence/local/verify-contract-20260823T123754Z.log`.
- Post-repair focused discovery/native-candidate tests — exit 0; 2/2 passed.
  Both sample executables passed directly with their stable simulated-protocol
  labels. The exact focused CTest selection passed 16/16, and both M17-06 gates
  passed 20 consecutive executions.
- Post-repair ASan/UBSan standalone target rebuild and direct execution — exit
  0 with no sanitizer or leak finding. The exact eight-test CTest selection was
  not clean in this sandbox: different attempts timed out in the sample, HAL-v2
  compatibility, or heterogeneous-memory tests while GCC libasan emitted only
  repeated `AddressSanitizer:DEADLYSIGNAL`. The same instrumented binaries
  passed direct execution, and diagnostic CTest runs adding only
  `ASAN_OPTIONS=verbosity=1` also passed, but failed exact attempts are not
  converted into passing evidence. No gate was weakened; mandatory hosted
  ASan/UBSan execution remains required.
- Post-repair installed-package consumer CTest — exit 0; 13/13 passed. C ABI
  v8 remained 70 exports with fingerprint `0xd0e7a5a14bf35f97`, and `readelf`
  still reported SONAME `librtfw.so.8`.
- Post-repair `./scripts/agent-verify.sh full` — exit 0. Contract checks passed,
  quick CTest passed 26/26, full CTest passed 30/30, and the 405-test aggregate
  passed inside the gate in 31.86 seconds. Retained log:
  `docs/evidence/local/verify-full-20260823T124713Z.log`.
- Final post-evidence `./scripts/agent-verify.sh contract` — exit 0; retained log
  `docs/evidence/local/verify-contract-20260823T124922Z.log`.

## Changed and preserved invariants

Changed: one Runtime-local capability-record initialization; exact discovery
and actual-candidate integration tests; one default non-installed portable
sample and one standalone gate; injected fixed simulated CUDA/XDMA protocols;
CI sanitizer/default/embedding/add-subdirectory coverage; repository state,
handoff, component contracts, readiness, and README; permanent future-safe
documentation/release checks; governance provenance; and reviewed release
source hashes.

Preserved: release 1.2.1; Apache-2.0; stable C ABI v8; exactly 70 exports;
fingerprint `0xd0e7a5a14bf35f97`; SONAME 8; every device ABI v1 declaration,
layout, value, and candidate `api()` behavior; HAL core v2; memory/topology
extension v1; command/timeline extension v1; extension ABI v1; Runtime status
values; runtime-profile schema 7 and 25 keys; observability/rate/checkpoint/
input-log/qualification schemas and IDs; installed SDK inventory and aliases;
six-row MemoryPlan and three provider regions; Runtime isolation; executor-
worker vendor-call prohibition; support matrices and qualification workflows;
the M17-05 retained evidence; and the portable RT0 claim boundary.

## Rollback

Stop accepting frames, use checked Runtime stop/retry to wake finite vendor
waits, retain uncertain owners until their existing drain/reset/shutdown
boundary, and release borrowed Graph/buffer/sample objects only after checked
cleanup. Then revert the single `command_batch.cpp` initialization repair,
focused discovery/native-registration tests, combined sample/test/CMake/CI
integration, documentation/governance, permanent checkers, release hashes, and
this M17-06 evidence as one bounded batch. No migration is required. The exact
baseline restores the retained M17-05 blocker and preserves M17-04 backend-local
behavior and both device-ABI-v1 candidate paths.

## Residual risks and unperformed validation

The simulated cancellation case exposes an intentional existing boundary:
Runtime can synthesize a finite timeout before a fixed XDMA worker publishes
its late canceled completion. The test uses bounded existing work to wake the
service owner and reap that candidate completion before corrected reuse. It
does not fabricate a timeline value or claim physical cancellation latency.

The intermittent post-review GCC libasan/CTest failure is an unresolved local
validation discrepancy. Direct instrumented execution and verbosity-only
diagnostics do not replace a clean exact gate, and no source-level sanitizer
finding or actionable stack was produced. Hosted ASan/UBSan remains mandatory.

Not performed or claimed: a clean post-review exact local combined ASan/UBSan
CTest selection; successful local ThreadSanitizer execution; hosted GitHub CI
or check identifiers; required human API, compatibility, concurrency, lifetime,
memory-order, accounting, security, or claim-boundary review; Windows MSVC,
supported GCC 11/Clang 14 hosted matrices, CUDA-toolkit compilation,
physical CUDA context/stream/device memory/pinning/coherency/Graph execution or
latency; physical XDMA node/BAR/DMA/event interrupt/PCI BDF/kernel driver/
firmware/bitstream/IOMMU/cancelability or latency; direct GPU-to-FPGA peer DMA;
combined hardware, HIL, controlled timing, field, thermal, endurance, RT1,
RT2, Unreal, signing, release, staging, deployment, production, or support-
promotion validation. No support tuple or matrix was changed. M17 and CAP-M17
must not be marked functionally complete until mandatory hosted CI and human
gates pass.

## CI repair: MSVC deliberate-alignment diagnostics

The supplied failure capture for pull-request CI run `32640625762` contained
only the statement that the run was still in progress, so it did not itself
identify a causal failure. Once the run completed, read-only job-log queries
for source HEAD `77d1901dee428fb87390750806753885202a8b03` identified the first
causal failure at `2026-08-23T12:53:48Z` in job `97197052053`, **build-windows
(RelWithDebInfo)**, step **Build**. Configuration had passed. MSVC v143 then
emitted C4324 for the deliberately 64-byte-aligned simulated CUDA allocation
and the two disjoint host-stage objects in `samples/cpu_gpu_fpga_cpu.cpp`;
`/WX` promoted the first warning to C2220. The standalone sample and focused
test target both reported the same diagnostic. Job `97197052069`, **Relocated
package consumer (windows-server-2022-x86_64-msvc-v143)**, later failed its
**Build package** step at `2026-08-23T12:54:24Z` for the same three source
locations after configuration, release-contract validation, and frozen C ABI
validation had passed. The later process failures and skipped test, archive,
manifest, relocation, and consumer steps are cascading results, not
independent causes.

The defect is classified as a portable-source/compiler-diagnostic defect with
high confidence. The bounded repair wraps only the two affected private sample
structures in MSVC warning push/disable/pop regions for C4324, matching the
repository's existing treatment of deliberate alignment padding. The
`alignas(64)` declarations, fixed object extents, disjoint host registrations,
post-start allocation instrumentation, simulated driver behavior, public and
installed surfaces, Runtime/HAL/device paths, tests, timeouts, and filters are
unchanged. The reviewed-source digest for
`samples/cpu_gpu_fpga_cpu.cpp` was refreshed in
`release/rtfw-release-contract.json`.

Exact commands and results for this repair:

- `gh run view 32640625762 --json status,conclusion,jobs` — exit 0 after run
  completion; enumerated exactly the two failed Windows jobs above.
- `gh api repos/kpbianco/cpp-rt-car/actions/jobs/97197052053/logs` and
  `gh api repos/kpbianco/cpp-rt-car/actions/jobs/97197052069/logs` — exit 0;
  established the first timestamped C4324/C2220 diagnostic and confirmed that
  the package failure was the same source defect.
- `command -v clang-cl`, `command -v x86_64-w64-mingw32-g++`, and
  `command -v cl` — each found no compiler. The exact Windows Server 2022 MSVC
  v143 `/W4 /WX` package build cannot run on this Linux worktree and remains a
  mandatory CI rerun; it is not inferred from local GCC validation.
- `cmake -S . -B build/m17-06-ci-repair-package
  -DCMAKE_BUILD_TYPE=Release -DENABLE_TESTS=OFF
  -DRTFW_BUILD_EXPERIMENTAL=OFF -DSIM_WERROR=ON` — exit 0.
- `cmake --build build/m17-06-ci-repair-package --target
  sample_cpu_gpu_fpga_cpu --parallel 2` — exit 0; the focused affected target
  compiled under the fresh local warnings-as-errors package configuration.
- `cmake --build build/m17-06-ci-repair-package --parallel 2` — exit 0; the
  complete local analogue of the failed package build passed.
- `./build/m17-06-ci-repair-package/samples/sample_cpu_gpu_fpga_cpu` — exit 0
  with the unchanged line `sample_cpu_gpu_fpga_cpu frames=2
  evidence=simulated_protocol physical_hardware=false
  direct_peer_dma=false`.
- `python3 tools/check_release_contract.py --write-hashes` — exit 0 and
  reported `Release contract OK` after refreshing the one changed reviewed
  source digest.
- Initial post-repair `./scripts/agent-verify.sh full` — exit 8 after contract,
  quick 26/26, both strict builds, and 29/30 full CTests passed. The unchanged
  405-test `simcore_all` aggregate reached its fixed 300-second timeout while
  entering
  `CrossMatrixDeterminism/SimCoreDeterminism.HashSameAcrossThreadCounts/2ThreadsFmaOff`.
  This is the same aggregate-only intermittent timeout already retained above;
  no M17-06 sample, compiler, sanitizer, or source diagnostic failed.
- `timeout 60s ./build/agent-full/tests/simcore_tests
  --gtest_filter='CrossMatrixDeterminism/SimCoreDeterminism.HashSameAcrossThreadCounts/2ThreadsFmaOff'`
  — exit 0; the exact test passed in 3.105 seconds.
- `timeout 120s ./build/agent-full/tests/simcore_tests` — exit 0; all 405
  cases completed in 27.796 seconds, with 401 passed and four expected skips.
- Final unchanged `./scripts/agent-verify.sh full` rerun — exit 0. Contract
  checks passed, quick CTest passed 26/26, full CTest passed 30/30, and the
  aggregate passed in 28.45 seconds. Retained log:
  `docs/evidence/local/verify-full-20260823T130801Z.log`.
- Post-evidence `./scripts/agent-verify.sh contract` — exit 0; documentation,
  release-contract/tooling, diff hygiene, and frozen C ABI v8 checks passed.
  Retained log:
  `docs/evidence/local/verify-contract-20260823T131007Z.log`.
- The aggregate runs recreated `chunk_cache_test.devbox`,
  `chunk_cache_test2.devbox`, and `trace.btrc`; these untracked generated test
  artifacts were removed before the final scope audit.

The repair produced no Windows, physical CUDA/XDMA, Graph, DMA, MMIO,
interrupt, HIL, controlled-timing, field, thermal, endurance, RT1, RT2,
Unreal, signing, release, deployment, production, support-promotion, or human
review evidence. M17 and CAP-M17 remain incomplete until the mandatory hosted
CI rerun and required human gates pass.

## CI repair follow-up: MSVC constant-condition diagnostic

The complete supplied log for pull-request CI run `32640625762` also contains
one independent warning after the deliberate-alignment diagnostics. At
`2026-08-23T12:53:49Z`, **build-windows (RelWithDebInfo)** reports MSVC v143
C4127 at `tests/test_cpu_gpu_fpga_cpu_sample.cpp:113`. The test passed the
compile-time constant expression `rtfw_combined_sample::graph_id == 7` to
`CHECK_TRUE`, whose runtime `if` makes MSVC diagnose a constant conditional
under `/W4`; `/WX` makes that diagnostic build-failing. The prior repair at
`075dcfe1d2a8cbe5038825fbf3668d4da3d6975d` suppresses only the three
intentional C4324 alignment diagnostics and therefore does not resolve C4127.
The later process exit and skipped test/package steps remain cascading
results. At current HEAD, C4127 is the first still-unrepaired causal diagnostic
from the supplied log.

The bounded follow-up replaces only that runtime macro invocation with
`static_assert(rtfw_combined_sample::graph_id == 7)`. This preserves and
strengthens the exact stable Graph-ID invariant at compile time without
disabling a diagnostic, weakening a test, changing test selection, or touching
production, public, vendor, Runtime, HAL, device-ABI, C-ABI, package, support,
or qualification behavior. The reviewed digest for
`tests/test_cpu_gpu_fpga_cpu_sample.cpp` is refreshed in
`release/rtfw-release-contract.json`.

Exact commands and results for this follow-up:

- `command -v cl`, `command -v clang-cl`, and
  `command -v x86_64-w64-mingw32-g++` found no compiler. The exact Windows
  Server 2022 MSVC v143 `/W4 /WX` build cannot run on this Linux worktree and
  remains a mandatory CI rerun; no Windows result is inferred from local GCC.
- `cmake -S . -B build/m17-06-ci-repair-focused
  -DCMAKE_BUILD_TYPE=RelWithDebInfo -DENABLE_TESTS=ON
  -DRTFW_BUILD_EXPERIMENTAL=ON -DSIM_WERROR=ON` — exit 0.
- `cmake --build build/m17-06-ci-repair-focused --target
  rtfw_cpu_gpu_fpga_cpu_sample_tests sample_cpu_gpu_fpga_cpu --parallel 2` —
  exit 0; both directly affected targets compiled with local warnings treated
  as errors.
- `ctest --test-dir build/m17-06-ci-repair-focused --output-on-failure -R
  'm17_cpu_gpu_fpga_cpu_sample|sample_cpu_gpu_fpga_cpu'` — exit 0; 2/2
  focused gates passed with the unchanged simulated-protocol behavior.
- `./scripts/agent-verify.sh full` — exit 0. Documentation, release, release-
  tool, diff-hygiene, and frozen C ABI v8 contract checks passed; quick CTest
  passed 26/26; full CTest passed 30/30; and the 405-test aggregate completed
  in 32.35 seconds. Retained log:
  `docs/evidence/local/verify-full-20260823T132015Z.log`.
- Post-evidence `./scripts/agent-verify.sh contract` — exit 0; documentation,
  release-contract/tooling, diff hygiene, and frozen C ABI v8 checks passed.
  Retained log:
  `docs/evidence/local/verify-contract-20260823T132159Z.log`.

This follow-up produces static, local GCC warnings-as-errors, unit,
simulated-protocol, and full local repository evidence only. It produces no
Windows, physical CUDA/XDMA, Graph, DMA, MMIO, interrupt, HIL, controlled-
timing, field, thermal, endurance, RT1, RT2, Unreal, signing, release,
deployment, production, support-promotion, or human-review evidence. M17 and
CAP-M17 remain incomplete pending mandatory hosted CI and human gates.

## CI failure capture 3: completed-run reconciliation

The supplied `M17-06-ci-failure-3.log` contains only the earlier observation
that pull-request CI run `32641728921` was still in progress. A read-only query
after completion shows that the run failed on source HEAD
`075dcfe1d2a8cbe5038825fbf3668d4da3d6975d`. The first causal failure is job
`97199738190`, **build-windows (RelWithDebInfo)**, step **Build**. Configuration
passed; at `2026-08-23T13:16:00Z`, MSVC v143 emitted C4127 for the constant
condition at `tests/test_cpu_gpu_fpga_cpu_sample.cpp:113`, and `/WX` promoted
it through C2220. The skipped test/SBOM/dependency steps and the canceled Debug
matrix build followed that build failure and are cascading outcomes, not
separate causal defects.

The clean branch entered this repair invocation at
`814a17eddda044ebd86be2a81083f52e9f87ad2d`, one commit after the failed SHA.
That commit already contains the authorized minimal repair documented above:
the runtime `CHECK_TRUE(rtfw_combined_sample::graph_id == 7)` invocation is a
compile-time `static_assert`, and the reviewed test-source digest is refreshed.
Because the supplied run predates that exact repair, no second source or test
change was appropriate. This reconciliation updates retained evidence only.

Exact commands and results for this reconciliation:

- `gh run view 32641728921 --json
  status,conclusion,headSha,headBranch,event,url,jobs` — exit 0; the run was
  complete with conclusion `failure`, HEAD
  `075dcfe1d2a8cbe5038825fbf3668d4da3d6975d`, and the first failed job was
  `97199738190`.
- `gh api
  repos/kpbianco/cpp-rt-car/actions/jobs/97199738190/logs` — exit 0; the first
  compiler diagnostic was C4127/C2220 at the test line above. The combined
  sample itself compiled successfully later in the same parallel build.
- `git show 814a17eddda044ebd86be2a81083f52e9f87ad2d --
  tests/test_cpu_gpu_fpga_cpu_sample.cpp release/rtfw-release-contract.json`
  — exit 0; confirmed the one-line assertion repair and matching digest update.
- `command -v cl`, `command -v clang-cl`, and
  `command -v x86_64-w64-mingw32-g++` found no compiler. Exact local MSVC v143
  reproduction was unavailable and is not inferred from GCC.
- Fresh configure in `build/m17-06-ci-failure-3-focused` with
  `RelWithDebInfo`, tests enabled, experimental code enabled, and
  `SIM_WERROR=ON` — exit 0.
- Focused build of `rtfw_cpu_gpu_fpga_cpu_sample_tests` and
  `sample_cpu_gpu_fpga_cpu` with `--parallel 2` — exit 0.
- Focused CTest regex
  `m17_cpu_gpu_fpga_cpu_sample|sample_cpu_gpu_fpga_cpu` — exit 0; 2/2 gates
  passed with unchanged simulated-protocol behavior.
- `./scripts/agent-verify.sh full` — exit 0. Contract and release checks passed,
  quick CTest passed 26/26, full CTest passed 30/30, and the 405-test aggregate
  completed within the gate. Retained log:
  `docs/evidence/local/verify-full-20260823T132914Z.log`.
- Post-evidence `./scripts/agent-verify.sh contract` — exit 0; documentation,
  release-contract/tooling, diff hygiene, and frozen C ABI v8 checks passed.
  Retained log:
  `docs/evidence/local/verify-contract-20260823T133051Z.log`.
- Read-only observation of current-HEAD CI run `32642216869` showed HEAD
  `814a17eddda044ebd86be2a81083f52e9f87ad2d`; Windows Debug **Build** and
  **Test**, the exact Windows RelWithDebInfo **Build** step that previously
  failed, and the relocated Windows package consumer had passed. The Windows
  RelWithDebInfo **Test** step and the complete run remained in progress.
  Those partial results are not represented as complete mandatory CI evidence.

This reconciliation adds static, local GCC warnings-as-errors, unit, and full
repository evidence only. It adds no complete current-head hosted CI, human
review, physical CUDA/XDMA, Graph, DMA, MMIO, interrupt, HIL, controlled-
timing, field, thermal, endurance, RT1, RT2, Unreal, signing, release,
deployment, production, or support-promotion evidence. M17 and CAP-M17 remain
incomplete pending every mandatory hosted and human gate.
