# M23-01 retained implementation and delivery-remediation evidence

Date: 2026-09-06 America/Phoenix (execution continues on September 7 UTC).
Baseline: `290397e86b65136c5479f7b1d69c961f73f8ac14`, merged PR #250.
Canonical control: `products/cpp-rt-car/batches/M23-01.yaml` at control revision
`e2c9c15b42f30c537bbf32b8e42290e4f55b5ba7`, blob
`f751f71073dca828ca434dbd0b3284a15c145427`. The compact target activation index
incorporates that complete canonical contract, including all manual gates.

## Recovery and publication boundary

The earlier portable-software-completion branch had no product tree delta.
No M23-M26 implementation could be recovered from it. The offline source
archive's SHA256SUMS and complete Git bundle were verified, then an isolated
checkout was created at the exact baseline with the exact GoogleTest gitlink
`7e17b15f1547bb8dd9c2fed91043b7af3437387f`. The initial worktree was clean.
GitHub branch creation succeeded, disproving the earlier read-only-tool claim.

This is newly implemented M23-01 code, not restored completion of M23-M26.
A recovery Git bundle is retained independently of the sandbox working tree.
Publication and merge identifiers must be verified independently of this file;
a source-import workflow or a saved bundle is not CI/human acceptance evidence.
No main-branch merge or hosted result is asserted by this local capture.

## Implementation mapping

- Optional `RTFW_BUILD_BENCHMARKS` host-side library/CLI and separately imported
  `rtfw::benchmark` component; default component inventory remains isolated.
- Size/versioned borrowed providers; copied frozen descriptions; deterministic
  selection; local generations; bounded capacities; explicit fake/steady clocks;
  exception containment, warm-up exclusion, counters and nearest-rank summaries.
- Closed canonical integer-only descriptor/raw/result schemas with exact
  source/build/host/workload/context and SHA-256 binding. Optional unavailable
  observations are explicit, never measured zero or invented facts.
- Atomic create-new three-file publication, Linux no-follow directory handles
  and renameat2 no-replace; Windows checked reparse/handle-based rename path.
  Offline schema/hash/arithmetic verification rejects malformed and substituted
  bundles. Authentication and crash/power-loss durability are not claimed.
- External-provider consumer, optional/default import isolation, standalone
  header and archive-relocation tests; installed CLI/schemas/validator/guide.
- Existing compiler/sanitizer/ABI/package gates extended rather than replaced;
  new cross-compiler constant-identity fixture exchange and optional SDK job.
- Stale active-frontier documentation corrected to merged M22 and candidate M23.
  Runtime, ABI, schemas, support matrices, license and prior evidence untouched.

## Local checks recorded so far

- GCC 14.2 C++20 direct compile with `-Wall -Wextra -Wpedantic -Wshadow -Werror`:
  passed for library/CLI and the initial 13 provider/runner tests.
- Supported nonexperimental CMake RelWithDebInfo build with benchmarks ON,
  tests ON, empty sanitizer set and warnings-as-errors: passed.
- Exact focused filter `BenchmarkContract.*:BenchmarkRunner.*:BenchmarkArtifacts.*`:
  19/19 passed, including POSIX short-write cleanup and concurrent no-replace.
- `ctest --test-dir build/m23-supported --output-on-failure -R
  'm23_benchmark|benchmark_package_consumer'`: 3/3 passed.
- Python offline artifact/CLI suite: 9/9 test groups passed, including required
  field mutations, duplicate/noncanonical/truncated/oversized JSON, rebound
  semantic mutations, cross-run hashes, missing/unlisted/symlink files and
  invalid CLI/overwrite rejection.
- Earlier full-build tool invocations were interrupted by command time limits;
  resumed builds completed. Interrupted invocations are not passing evidence.

Remaining local/hosted results are recorded below as they are actually produced.
No missing compiler, dependency, timing host, physical device or human review
is silently converted to success. GCC 14 is supplemental local evidence, not
proof of the required GCC 11, Clang 14 or MSVC tuples.

## Residual gates and rollback

Mandatory hosted compiler/sanitizer/determinism/default+optional-package,
portable-assurance/ABI/release checks and human schema/API, portability,
privacy/security, package/compatibility and claim review remain gates before
merge. M23-02 through M23-05, M24-M26, controlled-host statistical baselines,
CUDA/XDMA/HIL/RT1/RT2 qualification, Unreal, signing, release and deployment
remain unperformed and unclaimed.

Revert this optional component, integration, documentation and activation as
one bounded change. It creates no Runtime-state migration or support promotion.

## Additional measured local results

- Complete supported nonexperimental CTest selection: 36/36 passed.
- `./scripts/agent-verify.sh contract`: exit 0, including 35/35 release-tool
  tests, documentation/release digests, pinned dependency/actions and public
  cryptographic fixture validation. Fixture validation does not authenticate RTFW.
- Built C ABI: 70 symbols, exact frozen fingerprint and `librtfw.so.8` verified.
- Archive-relocated clean optional SDK: 18/18 consumer tests passed, including
  all prior consumers, standalone public-header compilation and external provider.
- Unrequested benchmark target isolation configured successfully against the
  same optional install; add-subdirectory provider consumer passed 1/1.
- First installation attempt deliberately retained its failure: installing from
  the test-enabled build included GoogleTest headers and failed the exact SDK
  inventory check. No inventory rule was relaxed. The documented clean package
  path uses tests OFF; a fresh archive/install at that setting passed 18/18.
- Clang 17 ASan+UBSan direct optional-component tests: 19/19 passed with
  `detect_leaks=0` and halt-on-error. This is not Clang 14 hosted or leak evidence.
- The official full script was invoked; its initial quick-build compilation was
  interrupted by the tool execution timeout. No successful full-profile result
  is claimed at this capture. Exact final script/CI status is a separate gate.

## Initial hosted validation and portability repair

The implementation was published as PR 251 at head
`ef579ceb877d75b2367a5528e36bb1423fa4717f`, with exact local/published tree
`1467491d8d0baae343064035e5fd86250bba0ebc`. Temporary transport files are
absent from that tree. The source transport itself is not test evidence.

CI run 34079137756 exposed a real Clang 14 `-Wconversion -Werror` failure
in the JSON encoder: a range-for converted signed `char` to `unsigned char`
implicitly. The repair reads `char` and casts explicitly before the ASCII
bounds check. Malformed-descriptor tests now also reject high-bit and UTF-8
input before invocation. No compiler warning or acceptance gate is disabled.

The repaired library, CLI, external consumer and three focused test sources
pass local Clang 17 syntax checks including `-Wconversion -Werror`. The
recompiled Clang 17 ASan/UBSan focused suite passes 19/19 (`detect_leaks=0`).
Initial-head GCC ThreadSanitizer and GCC/MSVC optional SDK jobs passed, but
they are not final repaired-head evidence. Full hosted validation, recorded
human review and the previously stated finalization gates remain required.
